def  oxford_comma(array)
  if(array.length == 1)
    return array.join()
  elsif(array.length == 2)
    return array.join(" and ")
  else
    array.length.times do |idx|
      if(idx + 1 == )
    end
  end
end